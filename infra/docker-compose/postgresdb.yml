networks:
  default:
    name: cr-postgres-net
    external: false

services:
  postgres:
    image: "postgres:17"
    container_name: cr-postgres-db
    restart: always
    environment:
      - PGDATA=/var/lib/postgresql/data/pgdata
      - POSTGRES_PASSWORD=mysecretpassword
    networks:
      - default
    volumes:
      - ../data/postgres/:/var/lib/postgresql/data:Z
